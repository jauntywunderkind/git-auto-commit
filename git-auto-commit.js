"use module"
import config from "./config.js"
import watchman from "./watchman.js"

export function main(){
	// get config
	// watch-project each-line-of-stdin & starting args
	// debounce
	// check for unstaged changes of checked in files
	// move to branch wip-${branchname}-date
	// make commit
}
